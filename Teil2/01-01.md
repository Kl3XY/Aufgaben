![[Pasted image 20251013100503.png]]

#todo 

## IPv4/IPv6
IP Adressen sind wie Hausadressen im echten Leben, jede sollte es in jeder Stadt nur einmal geben und führen immer zu einem Haus. 

Land: Server / Router
Stadt: Subnetz
Haus: IP-Adresse
#### IPv4
IPv4 ist der veraltete dennoch am häufigsten benutzte standard in dem Netzwerk teil der Informationstechnik. Diese besteht aus vier Oktette die jeweils darstellt was für eine Art IP-Adresse es ist und in welchen Subnetz sie befindet. Anhand einer Subnetzmaske wird festgelegt welcher teil der Adresse vom Host vergeben wird und welche den einzelnen Clients zugeordnet.

Subnetzmaske sind identisch aufgebaut wie normale IPv4 Adressen jedoch sind diese keine wirklich adressen mit welcher man sich verbindet sondern eher eine Vorgabe des Netzwerks welche Teile der Adresse zu Clients vergeben werden können. Dies wird gemacht um Netzwerke in einzelne Subnetze zu unterteilen. 

Eine normale öffentliche IP-Adresse würde ungefähr so aussehen
```
192.168.25.10
```

Mit einer Subnetzmaske die ungefähr so aussieht:
```
255.255.255.0
```

Um den genauen nutzen der Subnetzmaske klar zu machen müssen die jeweiligen IP-Adressen in Bits umgewandelt werden.
```
Host-Adresse
Dezimal : 192.168.25.10
Binär: 11000000.10101000.00011001.00001010

Binär-Adresse
Dezimal: 255.255.255.0
Binär: 11111111.11111111.11111111.00000000
```

Die Subnetzmaske definiert welcher teil der Host-Adresse sich innerhalb eines Subnetzes verstellen darf. Bei einer eins in einer Subnetzmaske bleibt der wert in der Host-Adresse starr und darf nicht verändert werden. Wenn dieser verändert wird, befindet sich die Adresse in einem anderen Subnetz.

Die Netzwerkadresse ist die erste Adresse in einem Netz.
Die Broadcast adresse ist die Letzte adresse in einem Netz.
Diese beiden Adressen ziehen vom allgemeinem Pool der verfügbaren Host-Adressen
#### IPv6
IPv6 ist moderner und löst die meisten probleme die wir haben mit IPv4 haben. IPv6 basier auf hextette (16 Bits in einem Abteil) und erlaubt dadurch zahlreich viel mehr Adressen als IPv4, wo wir bereits relativ am Limit stehen wie viele IPv4 Adressen wir noch haben. 

Beispiel einer IPv6 Adresse:
```
2600:1400:d:5a3::3bd4
```

## MAC
MAC Adressen sind eine Zusätzliche Adresse die jedem Gerät von derem Hersteller vergeben wird wo die erste Hälfte der Adresse der Identifier des Herstellers ist und die Zweite Hälfte das Gerät selbst beschreibt.

Beispiel einer Mac Adresse
```
2C:54:91:88:C9:E3
```

Broadcast Mac-Adresse
```
FF:FF:FF:FF:FF:FF
```
## Routing
Routing beschreibt das Leiten (bzw. Routing) Netzwerkpakete in entweder das Selber oder externen Subnetzen. Dies erfolgt über sogenannten Router die wichtige bestanndteile eines Netzwerks sind (muss ich wirklich erklären was ein Router ist cmon. Du hast 3 Jahre Ausbildung hinter dir, chill mal.)

## Switching

## ARP
Das Address Resolution Protocol (ARP) ist ein Netzwerkprotokoll was verwendet wird um Mac-Adressen an IP-Adressen zu heften und die Kommunikation über das Netzwerk zu verbessern.
Diese Mac-Address zu IP-Adressen Zuordnung werden in eine sogenannte "ARP-Tabelle" eingespeichert.

Dieses Protokoll wird nicht für IPv6 verwendet. Hierbei wird das "Neighbour Discovery Protocol" (NDP) verwendet.

## TCP
Tcp ist ein Berbindungsprotokoll was langsam, aber sicher, pakete an den Absender verschickt.

## UDP
Udp ist das genau gegenteil von tcp. Schnell und unsicher.
